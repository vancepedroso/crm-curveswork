{
  "name": "atoproof-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "//engines": "Pinned so a managed host doesn't pick a different major than local dev — bcrypt is a native module and is the thing most likely to break on a mismatch.",
  "engines": {
    "node": ">=22 <25"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "pg": "^8.13.0",
    "stripe": "^22.3.2",
    "xlsx": "^0.18.5"
  }
}
